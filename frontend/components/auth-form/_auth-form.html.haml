.auth_form
  = form_for login_path, method: :post do |f|
    = f.text_field :username, { class: "auth-form--input", placeholder: "Choose your username...", autofocus: true, required: true }
    = f.submit "Identify me", { class: "auth-form--submit" }
